{
    "version": 2,
    "buildCommand": "npm run build",
    "installCommand": "npm ci --include=dev",
    "outputDirectory": "dist/public",
    "routes": [
      {
        "src": "/(.*)",
        "dest": "/index.html"
      }
    ]
  }